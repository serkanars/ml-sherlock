version: 1

data:
  target: rented_bike_count
  train: data/train.csv
  production: data/production.csv

tracking:
  uri: sqlite:///mlflow.db
  experiment: example-seoul-bike

experiments:
  max_iterations: 6

report:
  output: output/report.html
