version: 1

data:
  target: ride_duration_minutes
  train: data/train.csv
  production: data/production.csv

tracking:
  uri: sqlite:///mlflow.db
  experiment: example-citi-bike

experiments:
  max_iterations: 6

report:
  output: output/report.html
